{
  "scripts": {
    "test": "node --test test/**/*.test.mjs",
    "dryrun": "arc-deploy --dry-run",
    "deploy": "arc-deploy",
    "destroy": "arc-destroy -f --app myapp"
  },
  "dependencies": {
    "@architect/architect": "^12.0.0",
    "@aws-sdk/client-cloudformation": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0"
  }
}
